apiVersion: v1
kind: Service
metadata:
  name: {{ include "reco_films.fullname" . }}
  labels:
    {{- include "reco_films.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reco_films.selectorLabels" . | nindent 4 }}
