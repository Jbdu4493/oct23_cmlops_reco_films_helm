apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reco_films.fullname" . }}-test-connection"
  labels:
    {{- include "reco_films.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reco_films.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
